{
  "name": "vegetables-before-candy-soroban",
  "version": "1.0.0",
  "description": "Stellar Soroban smart contracts for vegetables-before-candy",
  "scripts": {
    "build": "soroban contract build",
    "test": "soroban contract test",
    "deploy:testnet": "soroban contract deploy --network testnet",
    "deploy:mainnet": "soroban contract deploy --network mainnet"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.4",
    "soroban-cli": "^21.0.0"
  }
}